# serverless.yml

service: s3-db-lambda-retrieve

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: us-west-2

functions:
  app:
    handler: app.handler
    events:
      - http:
          path: get-parsed-expression
          method: post
          cors: true
      - http:
          path: test
          method: get
          cors: true